; The Hero video is stored through Spatie Media Library. File-size enforcement
; is intentionally set to 200 MB for the Hero video upload.
post_max_size = 200M
upload_max_filesize = 200M

; Large videos may take longer to upload than the PHP defaults allow.
max_execution_time = 0
max_input_time = 0
